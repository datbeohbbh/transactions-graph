syntax = "proto3";

package worker;

option go_package="/worker";

service Worker{
  rpc AddAddress(Address) returns (AddResp){}
}

message Address {
  string address = 1;
}

message AddResp {
  string msg = 1;
  bool status = 2;
}